
const activityWrapper = document.getElementById('activity-wrapper')

const a = document.querySelector('.circle .fill')
const rotation = Math.floor(Math.random() * 360)

// a.style.transform = 'rotate(' + rotation + 'deg)';

